NODE_ENV=test
# Dedicated test database — kept separate so tests never touch dev data.
DATABASE_URL=postgresql://taskmaster:taskmaster@localhost:5432/taskmaster_test
JWT_SECRET=test-secret-test-secret
