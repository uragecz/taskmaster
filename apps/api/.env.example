NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://taskmaster:taskmaster@localhost:5432/taskmaster
CORS_ORIGIN=http://localhost:3000
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRES_IN=7d

# Demo account created by `pnpm seed` (dev only — never seed this in production)
SEED_USER_EMAIL=demo@taskmaster.dev
SEED_USER_PASSWORD=demo1234
