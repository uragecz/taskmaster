# Empty -> the browser calls the API on the same origin (/api/*), forwarded to
# the API by the gateway (prod) or a Next.js rewrite (dev). Set an absolute URL
# only if the API lives on a different origin.
NEXT_PUBLIC_API_URL=
